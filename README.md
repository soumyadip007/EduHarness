# EduHarness

EduHarness is a pedagogical harness for trustworthy agentic tutoring.

## Quick start

1. Copy `.env.example` to `.env` and fill required keys.
2. Install dependencies: `make install`
3. Run API: `make dev`
4. Health check: `GET /health`
