export default function HomePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>EduHarness Frontend</h1>
      <p>Use <code>/student</code> for student tutoring UI.</p>
      <p>Use <code>/student/sessions</code> for session history.</p>
    </main>
  );
}
